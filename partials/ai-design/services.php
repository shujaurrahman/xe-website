<?php /* Services & packages — the shared catalogue (one component, identical on every page, never restyled here).
   Hub data key: 'ai-design' in data/services/ai-design.php. Sits immediately before the FAQ. */
$svc_key = 'ai-design';
include __DIR__ . '/../services/catalogue.php';
