<?php /* DRAFT COPY — review before launch */
/* Services & packages — the shared catalogue (one component, identical on every page). Hub data key: 'ai-design'.
   Immediately before the FAQ. */
$svc_key = 'ai-design';
include __DIR__ . '/../services/catalogue.php';
